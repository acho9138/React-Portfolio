import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles((theme) => ({
  textContainer: {
    padding: "15px",
    margin: "10px"
  },
  img: {
    maxWidth: '100%'
  },
}));
